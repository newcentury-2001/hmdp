**业务功能完善**

- ~~增加“候补/排队”，售罄后支持候补登记。~~
- ~~完善限购规则：每人每券限购、每 IP/设备限流、黑白名单、人群筛选（会员等级、地理区域）。~~
- ~~秒杀入口校验更完整：活动状态、时间窗口、券有效期、店铺状态、用户资格（实名、绑定手机号、信用评分）。~~
- ~~下单体验优化：下单结果异步轮询接口、排队提示与预计等待时间、统一错误码与友好提示。~~
- 加入风控：行为评分、图形验证码/轻量 PoW、异常阈值触发强验证或封禁。

**优惠券体系完善**

- 券模板与实例分离：模板定义类型（满减、折扣、直减、秒杀券）、适用范围（分类/店铺）、叠加/排他规则、使用门槛。
- 统一核销流程：订单核销、退券回补、部分退款场景的券处理。
- 券码/发放渠道：支持口令券、渠道发放、发放上限与动态扩容。
- 监控与报表：券领取转化率、核销率、售罄速度、异常峰值。

**Redis 设计优化**

- ~~原子扣减与资格校验整合到 Lua：一次性完成“资格校验 + 预扣库存 + 幂等标记”，避免多次往返与竞态。~~
- ~~Key 设计与分片：`seckill:stock:{voucherId}`、`seckill:uid:{voucherId}`；热点券进行分片库存（`{voucherId}:{shard}`），或采用单分区顺序处理（见 Kafka 设计）。~~
- 过期策略与预热：活动开始前 `TTL` 预热，活动结束后统一清理；脚本和热点 key 预热到 Redis。
- ~~幂等与防重：用 `SETNX` 或 Lua 将 `userId+voucherId` 标记为已参与；回源时严格检查。~~
- ~~限流与滑动窗口：用 `ZSET` 或 `bucket` 实现按 IP、设备的滑动窗口限流；~~
- 服务层加入漏斗/令牌桶。
- ~~锁的使用收敛：避免大范围分布式锁；确需锁时用 Redisson 可重入锁与看门狗，限定微小临界区。~~

**用 Kafka 替代 Redis Streams**

- ~~Topic 设计：`voucher.seckill.request`（下单请求）、`voucher.seckill.result`（结果通知）、`order.timeout`（支付超时）；关键业务独立 topic。~~
- ~~分区策略：用 `voucherId` 作为消息 key，保证同券请求顺序性并降低并发竞态；热点券可增分区或采用单分区串行扣减。~~
- ~~生产者可靠性：`acks=all`、`retries`、`delivery.timeout.ms`、开启幂等生产者；必要时使用事务性生产者。~~
- ~~消费者幂等与提交：手动偏移提交；落库前先做幂等校验（`userId+voucherId` 唯一约束/幂等表）；失败重试与死信队列（DLQ）。~~
- ~~事务一致性：订单服务采用 Outbox 模式（本地事务写订单+Outbox；后台转发到 Kafka），或在消费侧使用本地事务保证“扣减库存与创建订单”的一致性。~~
- 回压与降级：消费者端速率限制、批量消费与批量持久化；队列积压告警与自动降级（暂停入口或切静态页）。

**性能与扩展性**

- 热点规避：热点券采用单分区顺序扣减或库存分片；读路径使用旁路缓存与本地缓存（Caffeine）。

**可观测性与运维**

- 指标：库存准确率、订单创建成功率、排队拒绝率、P99 延迟、队列积压量、消费者滞后、错误率。
- 日志与追踪：结构化日志、TraceId 贯穿；对关键路径打点（入口、Lua 扣减、消息生产/消费、落库）。
- 告警与自愈：库存异常回补、队列积压降级、失败重试阈值告警；一键关闭秒杀入口的开关。

**VoucherOrderServiceImpl 改造落点**

- 入口校验重构：抽离 `validateSeckillRequest(userId, voucherId)`，检查活动/限购/风控等。
- ~~异步化下单：入口只负责快速校验与发送 `voucher.seckill.request`，立即返回排队态或受理成功。~~
- ~~状态机与支付超时：订单表增加 `status` 与 `expire_time`；生产 `order.timeout` 消息或使用延迟队列处理超时取消与库存回补。~~
- ~~失败与回滚：落库失败或业务失败时，发布补偿事件或在消费端回滚预扣库存。~~
- 观测埋点：在入口、发送消息、消费、落库、状态流转处打埋点与日志。

**数据表与模型调整建议**

- 优惠券/库存表：拆分“总库存 / 可售库存 / 预扣库存”；便于回补与报表。
